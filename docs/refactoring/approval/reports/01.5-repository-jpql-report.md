# Stage 1.5 작업 보고서: Repository 상태 리터럴 정합

작성일: 2026-07-28

---

### 수정 파일 (+추가 -삭제 라인 수)

| 파일 | 변경 |
|---|---|
| `approval/repository/ApprovalRepository.java` | +0 -0 (라인 수 동일, 리터럴 교체만) |
| `approval/repository/ApproverRepository.java` | +4 라인(import + 주석 3줄), -0 (리터럴 교체 + 메서드 개명) |
| `approval/service/ApprovalService.java` | +0 -0 (호출부 1줄 내 교체) |

---

### 교체한 JPQL 리터럴 (전체 경로 Enum 상수)

**ApprovalRepository.java**

| 라인 | 기존 | 교체 후 |
|---|---|---|
| L19 | `!= '임시저장'` | `!= com.insider.login.approval.enums.ApprovalStatus.TEMP_SAVED` |
| L25 | `!= '임시저장'` | `!= com.insider.login.approval.enums.ApprovalStatus.TEMP_SAVED` |
| L31 | `!= '임시저장'` | `!= com.insider.login.approval.enums.ApprovalStatus.TEMP_SAVED` |
| L35 | `= '임시저장'` | `= com.insider.login.approval.enums.ApprovalStatus.TEMP_SAVED` |
| L39 | `= '임시저장'` | `= com.insider.login.approval.enums.ApprovalStatus.TEMP_SAVED` |
| L43 | `= '임시저장'` | `= com.insider.login.approval.enums.ApprovalStatus.TEMP_SAVED` |

**ApproverRepository.java**

| 라인 | 기존 | 교체 후 |
|---|---|---|
| L29 | `= '대기'` (JPQL) | `= com.insider.login.approval.enums.ApproverStatus.PENDING` |

---

### 교체한 네이티브 쿼리 리터럴 (DB 저장 문자열)

**ApproverRepository.java (`findStandByApprovalsByTitleNative`)**

| 라인 | 기존 | 교체 후 |
|---|---|---|
| L40 | `d.approver_status = '대기'` | `d.approver_status = 'PENDING'` |
| L45 | `c.approval_status = '처리 중'` | `c.approval_status = 'PROCESSING'` |

개명 취약성 주석 추가: **Y**

```java
// 주의: 네이티브 쿼리이므로 Enum 상수와 연결되지 않는다.
// 'PENDING' = ApproverStatus.PENDING, 'PROCESSING' = ApprovalStatus.PROCESSING 의 DB 저장값.
// Enum 상수 개명 시 이 리터럴도 함께 수정해야 함. (Stage 6에서 JPQL 재작성 검토)
```

---

### 시그니처 변경

- `findByApprovalNoAndApprovalStatus` → `findByApprovalNoAndApproverStatus`
  - 파라미터 타입: `String` → `ApproverStatus`
  - 호출부 수정 위치: `ApprovalService.java:409`

---

### 임시 수정 파일 (단계 6에서 제거 예정)

**ApprovalService.java**

- L409: `approverRepository.findByApprovalNoAndApprovalStatus(approvalNo, "반려")` → `approverRepository.findByApprovalNoAndApproverStatus(approvalNo, ApproverStatus.REJECTED)`

### 임시 수정 라인 합계

ApprovalService: **1라인** / 허용 15라인

---

### 빌드 결과

```
cd final && .\gradlew.bat compileJava: BUILD SUCCESSFUL (17s)
```

### 부팅 결과

```
cd final && .\gradlew.bat bootRun: 사용자 수동 확인 필요
```

---

### 한글 리터럴 잔존 확인

- Repository 계층: **0건** (주석 제외)
- Service 계층: 다수 건 (단계 6에서 처리 예정, 정상)
- Controller 계층: 다수 건 (단계 3·7에서 처리 예정, 정상)

---

### Success Criteria 확인

| # | 항목 | 결과 |
|---|---|---|
| 1 | compileJava 통과 | ✅ BUILD SUCCESSFUL |
| 2 | bootRun 정상 기동 | 사용자 확인 필요 |
| 3 | ApprovalRepository 한글 리터럴 0건 | ✅ |
| 4 | ApproverRepository 한글 리터럴 0건 (JPQL·네이티브 모두) | ✅ |
| 5 | JPQL 전체 패키지 경로 표기 | ✅ |
| 6 | 네이티브 리터럴 'PENDING'/'PROCESSING' 교체 + 경고 주석 | ✅ |
| 7 | 메서드 개명 및 타입 변경 | ✅ `findByApprovalNoAndApproverStatus(String, ApproverStatus)` |
| 8 | Service 호출부 Enum 사용 | ✅ `ApproverStatus.REJECTED` |
| 9 | 시그니처 변경 1건뿐 | ✅ |
| 10 | 임시 수정 기록 | ✅ ApprovalService:409, 1라인 |
| 11 | 범위 외 파일 변경 없음 | ✅ |

---

### 확인 필요 사항

- `bootRun` 기동 후 수동 API 검증 필요:
  - 임시저장함 조회 API → 데이터 반환 확인 (기존: 빈 목록)
  - 결재 대기함 조회 API → 데이터 반환 확인 (기존: 빈 목록)
  - 임시저장함 제목 검색 → 검색 결과 반환 확인
  - 임시저장함 카운트 → 0이 아닌 값 확인
  - 반려된 결재 상세 조회 → 반려 처리일시 표시 확인
